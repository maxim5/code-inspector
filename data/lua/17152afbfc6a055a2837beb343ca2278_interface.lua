
-- a wip interface

interface = {}


function interface.init()
	print( "Initialized interface." )
end

function interface.terminate()
	print( "Terminated interface. " )
end

function interface.addInstrument( file )
end

function interface.rmInstrument( file )
end

function interface.assignInstrument( instrument, keys )
	--assigns an instrument to certain keys
end
